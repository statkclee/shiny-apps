---
title: "카드 Shiny 앱"
format: html
editor: source
output:
  html_document:
    theme:
      version: 4
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
## [GitHub Repo.] statkclee/shiny-apps

knitr::opts_chunk$set(echo = FALSE, message=FALSE, warning=FALSE,
                      comment="", digits = 3, tidy = FALSE, prompt = FALSE, fig.align = 'center')
library(tidyverse)
library(rvest)
library(lubridate)
```




```{r card-groups}
library(bs4cards)

shiny_list <- list(
  tibble(
    title = "원주율(π)",
    image = "assets/images/life-of-pi.gif",
    text = "몬테카를로 모의시험 방법으로 원주율 계산 (Shiny App)",
    link = "https://dl-dashboard.shinyapps.io/life_of_pi/",
    footer = "2022-06-02"
  ),
  tibble(
    title = "실용통계",
    image = "assets/hsData-favicon-180x180.png",
    text = "실용통계 데이터셋",
    link = "https://statkclee.github.io/shiny-apps/flexdashboard/hsData.html",
    footer = "2022-06-03"
  ),
  tibble(
    title = "숫자 맞추기",
    image = "assets/favicon-guess-number.png",
    text = "기회는 3번, 1~20사이 숫자 맞추기",
    link = "https://dl-dashboard.shinyapps.io/guess_number/",
    footer = "2022-06-04"
  )
)

shiny_tbl <- shiny_list %>% 
  enframe() %>% 
  unnest(value)

shiny_tbl %>% 
  cards(
    title = title,
    text = text,
    link = link,
    image = image,
    footer = footer,
    layout = "label-left", 
    width = 3
  )
```


# 개발방법 

[bs4cards](https://bs4cards.djnavarro.net/articles/articles/layouts.html)

